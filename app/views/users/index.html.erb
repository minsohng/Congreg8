<% provide(:title, 'All users') %>
<h1>All users</h1>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <% if current_user.admin? && !current_user?(user) %>
        <%= user %>
      <% end %>
    </li>
  <% end %>
</ul>
